// parh = \YanderePy\Pygame runtime\YanderePy_System
// YandereMusic.cpp

#include<iostream>
#include"YanderePy.h"
#include"CppGame.hpp"
#include"CppGame.h"
#include"YandereMusic.h"
#include



class MainProgram{ // Start the program

    void RenderPyOpenGL(bool)
	{
		bool PyOpenGLinitialized = true
		if (PyOpenGLinitialized)
		{
			std::cout<< "Initialized pyopengl from pyopengl.hpp" <<std::endl;
		}else{
			std::cout<< "System > error cannot initialize pyopengl error may occured while initializing from pyopenglAssemblies.hpp"<<std::endl;
		}
	}
	void RenderPyGame(float)
	{
		float X = 10.2
		float Y = 20.2
		float Z = 5.2
	}
	void RenderPyGame(bool)
	{
		bool PyGameinitialized = true
		if (PyOpenGLinitialized)
		{
			std::cout<< "Initialized pygame from pygame.h" <<std::endl;
		}else{
			std::cout<< "System > error cannot initialize pygame error may occured while initializing from pygameAssemblies.hpp"<<std::endl;
		}
	}
	private: //https://www.youtube.com/watch?v=6G7ilxWNG9o
	    bool pyopenglInitialized = true;
	public:
	    bool PygameInitialized = true;
}
class StartProgram{
	private:
	    float pyopenglFov = 3.2;
	public:
	    bool MurderSpeed = true;
		float KillSpeed = 10.2;
		
}
int main()
{
	std::cout <<"Hello From YanderePy.cpp and YanderePy.dll"<<std::endl;
	return 0;
}
